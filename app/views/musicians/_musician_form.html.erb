<%= form_for @musician do |f| %>
  <%= f.label :name, "" %>
  <%= f.text_field :name, placeholder: "Name", class: "form-control" %> <!-- to style the form -->
  <%= f.submit nil, class:"btn btn-primary", id:"submit" %> <!-- to style the button -->
<% end %>